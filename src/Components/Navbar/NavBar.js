import React from 'react';
import logo from '../../Assets/logo.png'

const NavBar = () => {
    return (
        <div>
            <img src={logo} alt="" />
        </div>
    );
};

export default NavBar;